# SCMSYS Workspace

This repository now tracks every project from a single `SCMSYS/` root to keep GitHub clean and predictable.

```
SCMSYS/
├── automation/   # OCI Terraform + observability helpers (see ./automation/README.md)
└── zeroclaw/     # Upstream ZeroClaw runtime snapshot for local builds/tests
```

Add future stacks as new folders directly under `SCMSYS/` to keep the layout flat.
