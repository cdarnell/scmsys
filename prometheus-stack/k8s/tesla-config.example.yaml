apiVersion: v1
kind: ConfigMap
metadata:
  name: tesla-config
data:
  # Interval between polls in seconds
  TESLA_POLL_INTERVAL: "30"
  # Kafka settings
  KAFKA_BOOTSTRAP_SERVERS: "localhost:9092"
  KAFKA_TOPIC: "tesla.telemetry"
  # Prometheus metrics port
  PROMETHEUS_PORT: "8000"
